components:
  schemas:
    RomanNumeralResponse:
      type: object
      properties:
        input:
          type: string
          example: "1"
        output:
          type: string
          example: "I"
